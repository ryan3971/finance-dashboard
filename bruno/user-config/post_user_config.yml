info:
  name: post_user_config
  type: http
  seq: 3

http:
  method: POST
  url: "{{localhost}}/api/v1/user-config/reset"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Auth: Authorization: Bearer <token>

  Valid update

  // Request body
  {
    "allocations": {
      "needsPercentage": 50,
      "wantsPercentage": 30,
      "investmentsPercentage": 20
    }
  }
  Expected: 200 with the full user config object including the new fields:


  {
    "id": "...",
    "userId": "...",
    "emergencyFundTarget": null,
    "needsPercentage": 50,
    "wantsPercentage": 30,
    "investmentsPercentage": 20,
    "updatedAt": "..."
  }
  Percentages don't sum to 100

  { "allocations": { "needsPercentage": 50, "wantsPercentage": 30, "investmentsPercentage": 30 } }
  Expected: 400

  Empty body (no-op)

  {}
  Expected: 200 — returns existing config unchanged

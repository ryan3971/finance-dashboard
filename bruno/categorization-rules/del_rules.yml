info:
  name: del_rules
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{localhost}}/api/v1/categorization-rules/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

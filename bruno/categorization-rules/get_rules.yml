info:
  name: get_rules
  type: http
  seq: 5

http:
  method: GET
  url: "{{localhost}}/api/v1/categorization-rules"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: unmark
  type: http
  seq: 3

http:
  method: POST
  url: "{{localhost}}/api/v1/transfers/unmark"
  body:
    type: json
    data: |-
      {
        "transactionId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

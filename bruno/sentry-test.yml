info:
  name: sentry-test
  type: http
  seq: 9

http:
  method: GET
  url: "{{localhost}}/debug-sentry"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

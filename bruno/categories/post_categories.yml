info:
  name: post_categories
  type: http
  seq: 2

http:
  method: POST
  url: "{{localhost}}/api/v1/categories"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

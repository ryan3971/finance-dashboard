info:
  name: del_categories
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{localhost}}/api/v1/categories/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

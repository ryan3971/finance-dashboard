info:
  name: patch_categories
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{localhost}}/api/v1/categories/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: load_seed
  type: http
  seq: 6

http:
  method: GET
  url: "{{localhost}}/api/v1/seed/load"
  headers:
    - name: ""
      value: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: "{{localhost}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "test2@gmail.com",
        "password": "password123"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("token", res.body.accessToken);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

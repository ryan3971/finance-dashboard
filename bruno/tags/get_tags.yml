info:
  name: get_tags
  type: http
  seq: 1

http:
  method: GET
  url: "{{localhost}}/api/v1/tags"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: delete_tag
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{localhost}}/api/v1/tags/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

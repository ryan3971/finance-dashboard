info:
  name: get_account
  type: http
  seq: 2

http:
  method: GET
  url: "{{localhost}}/api/v1/accounts"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

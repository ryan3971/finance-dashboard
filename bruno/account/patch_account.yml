info:
  name: patch_account
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{localhost}}/api/v1/accounts/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |2
        {
          "name": "CIBC Mastercard",
          "type": "credit",
          "institution": "cibc",
          "isCredit": true
        }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

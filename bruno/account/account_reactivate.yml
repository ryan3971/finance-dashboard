info:
  name: account_reactivate
  type: http
  seq: 5

http:
  method: POST
  url: "{{localhost}}/api/v1/accounts/:id/reactivate"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |2
        {
          "name": "CIBC Mastercard",
          "type": "credit",
          "institution": "cibc",
          "isCredit": true
        }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: account_deactivate
  type: http
  seq: 4

http:
  method: POST
  url: "{{localhost}}/api/v1/accounts/:id/deactivate"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |2
        {
          "name": "CIBC Mastercard",
          "type": "credit",
          "institution": "cibc",
          "isCredit": true
        }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

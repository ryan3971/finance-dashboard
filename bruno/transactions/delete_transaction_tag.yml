info:
  name: delete_transaction_tag
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{localhost}}/api/v1/transactions/:id/tags/:tagId"
  params:
    - name: id
      value: ""
      type: path
    - name: tagId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: patch_transaction
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{localhost}}/api/v1/transactions/:id"
  params:
    - name: id
      value: test
      type: path
  body:
    type: json
    data: |-
      {
        "categoryId": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
        "subcategoryId": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
        "needWant": "Need",
        "note": "Monthly groceries",
        "createRule": true,
        "isInvestmentContribution": false
      }
  auth:
    type: bearer
    token: "{{token}}"

# Response shape (200):
# {
#   ...transactionFields,
#   retroactivelyApplied: number   ← other matching transactions updated when createRule:true created a new rule
# }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

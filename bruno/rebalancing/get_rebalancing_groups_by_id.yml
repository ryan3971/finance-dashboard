info:
  name: get_rebalancing_groups_by_id
  type: http
  seq: 3

http:
  method: GET
  url: "{{localhost}}/api/v1/rebalancing/groups/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: get_rebalancing_groups
  type: http
  seq: 6

http:
  method: GET
  url: "{{localhost}}/api/v1/rebalancing/groups"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

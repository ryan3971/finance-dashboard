info:
  name: delete_rebalancing_groups_by_id_transaction
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{localhost}}/api/v1/rebalancing/groups/:id/transactions/:transactionId"
  params:
    - name: id
      value: ""
      type: path
    - name: transactionId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

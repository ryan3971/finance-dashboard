info:
  name: get_investment_transactions
  type: http
  seq: 1

http:
  method: GET
  url: "{{localhost}}/api/v1/investments/transactions?page=1&pageSize=50"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

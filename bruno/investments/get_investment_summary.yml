info:
  name: get_investment_summary
  type: http
  seq: 3

http:
  method: GET
  url: "{{localhost}}/api/v1/investments/summary?year=2026"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

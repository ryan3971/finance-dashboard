Create a new branch off `main` called `<branch-name>`, commit all current changes, push, and open a PR.

**Environment:**

- Working directory: `<absolute path to repo root or worktree>`
- Running in a worktree: yes / no
- `node_modules` installed in working directory: yes / no
- `.env` present at `apps/api/.env` in working directory: yes / no
- `gh` CLI location: `C:\Program Files\GitHub CLI\gh.exe` (or: confirm it's on PATH)

**PR details:**

- Title: `<one-line title>`
- Summary: `<bullet points describing what changed and why>`
- Test plan: `<what to verify>`
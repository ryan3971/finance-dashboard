#!/bin/sh
output=$(pnpm test 2>&1)
exit_code=$?

if [ $exit_code -ne 0 ]; then
  echo "$output" | grep -E "FAIL |Error:|No test suite found" | head -20 >&2
  echo "" >&2
  echo "Tests failed — push aborted. Run 'pnpm test' for full output." >&2
  exit 1
fi
